name = "脱敏个人 Bug 日报 fixture"
rrule = "FREQ=DAILY;BYHOUR=8;BYMINUTE=0"
cwds = ["${PROJECT_ROOT}"]
prompt = """
默认执行模式：读取 ${PROJECT_ROOT}/tests/fixtures/config/valid.yaml 的 personal.scopeNames，
并使用 scripts/render-report.py --mode personal 生成 v2 个人报告。
"""

[target]
project_id = "${PROJECT_ROOT}"
