# Issue #26 真实 ZenTao E2E 记录

目标实例：ZenTao 21.7.8（真实地址和凭据不写入仓库）。

Issue 场景通过已认证的 `gh issue view 26 --repo wwtweiwenting/zentao-ai-assistant --json number,title,state,body,comments` 读取；执行入口为项目公开 CLI。每个场景按编号顺序执行，写入后显式回读；`UNKNOWN_WRITE_RESULT` 不自动重放。

公共 fixture：CRM 产品 `3`、CRM 项目 `8`、Iteration A `9`、Iteration B `10`、CRM 系统 `3`；测试用户账号均为本轮唯一账号，密码不记录。场景失败或诊断产生的叶子 Bug 只按明确 ID 清理。

## 场景记录

### BUG-GH-001

- 测试结果：通过。
- 发现的问题：首次实现创建的 Bug 回读产品为 `0`；真实 `bug.create` 请求只发送了官方 `productID`，21.7.8 还要求兼容字段 `product`。另一次关闭请求返回 `UNKNOWN_WRITE_RESULT`。
- 根因：`BugsAPI.create` 未发送 21.7.8 所需的 `product` 别名；关闭结果不确定属于网络确认边界。
- 修改内容：Bug 创建同时发送 `productID` 和 `product`；同步 Fake 路由、合同回归测试、catalog 兼容元数据、reference 和 21.7.8 evidence。关闭未重放。
- 自动化测试结果：目标 catalog/contract/Fake/scenario 测试 18/18 通过。
- 场景复测结果：唯一目标 Bug #6 创建后回读 `product=3`、`severity=2`、负责人为张三；resolve 回读为 `resolved/fixed/resolvedBuild=9/assignedTo=小王`；close 初始未知，立即回读确认 `closed/fixed`，未创建第二个目标 Bug。

### BUG-GH-002

- 测试结果：通过，含一次真实版本兼容性分支。
- 发现的问题：第一次按自然语言意图执行 activate 时未带版本，21.7.8 返回 `『影响版本』不能为空`；回读确认 Bug 仍为 `resolved`，没有错误生命周期变化。
- 根因：目标版本的 `bug.activate` 实际要求 `openedBuild`，虽官方参数和当前 CLI 合同标为可选；这是目标 ZenTao 版本差异，不是可用吞错或重试解决的错误。
- 修改内容：将真实观察登记为 `bug.activate` 的 21.7.8 compatibility evidence，并在 reference/catalog notes 说明调用方需从已回读的当前解决/影响版本显式传入；未修改官方合同、未自动 GET 或重放写请求。
- 自动化测试结果：compatibility、official snapshot、contract 6/6 通过。
- 场景复测结果：唯一目标 Bug #7 按 `fixed/2.3.2/小王` 回读后，激活重试使用已确认的构建 ID 10，回读为 `active/张三` 且备注保留“连续点击两次仍然会重复提交”；再次 resolve 为 `fixed/2.3.3/小王`，最终 close 后回读为 `closed/fixed`，整个过程只有 Bug #7。

### BUG-GH-003

- 测试结果：通过，含同一 21.7.8 `openedBuild` 兼容处理。
- 发现的问题：无新的源码问题；每次 activate 都必须显式带上上一阶段回读确认的构建 ID。
- 根因：目标实例的 activate 校验要求 `openedBuild`，与官方“可选”参数描述存在版本差异。
- 修改内容：无运行时代码修改；沿用已记录的 `bug.activate` compatibility evidence，使用回读得到的 12、13 作为两次驳回的 `openedBuild`，未猜测版本。
- 自动化测试结果：生命周期 contract/CLI E2E 5/5 通过。
- 场景复测结果：唯一目标 Bug #8 完成两次 `resolved→active` 驳回，备注分别记录 Firefox 失败及“正常模式好了，但无痕模式仍报错”；第三次以构建 14 `fixed` 解决并交小王，最终回读为 `closed/fixed`，未创建新 Bug。

### BUG-GH-004

- 测试结果：通过。
- 发现的问题：无新的源码问题；activate 继续遵守已记录的目标版本兼容要求。
- 根因：目标实例的 activate 仍要求显式 `openedBuild`。
- 修改内容：无运行时代码修改；使用初始化闭环回读的原影响版本 9 执行重新激活。
- 自动化测试结果：沿用已通过的生命周期 contract/CLI E2E 5/5，未改变源码。
- 场景复测结果：同一个 Bug #9 真实历史为 `closed/fixed → active → resolved/fixed(resolvedBuild=15) → closed/fixed`，激活后负责人为张三、解决后交小王，未创建内容相似的替代 Bug。

### BUG-GH-005

- 测试结果：通过。
- 发现的问题：无新的源码问题；场景要求“先别关”时不能执行 close，且目标版本 activate 仍要求显式影响版本。
- 根因：21.7.8 的 `bug.activate` 对 `openedBuild` 有实际校验；本场景前置的已解决 Bug 已有可回读的影响版本 15。
- 修改内容：无运行时代码修改；使用前置 Bug #10 的已回读影响版本 15 执行一次 activate，未执行 close、未重放写请求。
- 自动化测试结果：contract/CLI E2E 5/5 通过。
- 场景复测结果：同一个 Bug #10 从 `resolved/fixed/小王` 激活为 `active`，负责人回读为张三，激活备注为“这个问题还是存在，退回张三继续修”，历史中没有 close 动作。

### BUG-GH-006

- 测试结果：通过。
- 发现的问题：无新的源码问题；版本 4.0.1 需先建立真实 Build 供 resolve 选择。
- 根因：无；Build #16 是本场景的真实测试数据准备。
- 修改内容：创建并回读 Build `4.0.1`（ID 16）；同一个 Bug #10 使用 `resolve` 指派小李，未把负责人错误设为小王。
- 自动化测试结果：contract/CLI E2E 5/5 通过。
- 场景复测结果：#10 回读为 `resolved/fixed/resolvedBuild=16/assignedTo=gh26_xiaoli_20260825`，随后同一 Bug 执行 close 并回读为 `closed/fixed`；历史包含小李验证通过的关闭备注。

### BUG-GH-007

- 测试结果：通过；中途发现并修复一个真实兼容性/合同问题后复测通过。
- 发现的问题：首次将“只转交负责人”映射为 `bug edit --assignee` 时，真实请求 `PUT /api.php/v2/bugs/11` 返回 HTTP 成功但空 body，CLI 报 `API_ERROR`；回读仍为 `resolved/assignedTo=小王`。使用只改标题的请求在 resolved Bug #11 和 active 诊断 Bug #12 上同样空响应且回读不变。
- 根因：`bug.edit` 适配器错误暴露并发送了官方合同没有的 `assignedTo`；目标 21.7.8 的该 PUT 编辑端点实际不可用，不能承担负责人转交。
- 修改内容：移除 `BugsAPI/BugsService/bug edit` 的 `assignee` 参数和 CLI 选项；同步 endpoint catalog、官方合同快照、Fake 样例、兼容性 evidence 与 21.7.8 文档，并增加 CLI 误用不发 HTTP 的回归测试。已确认的负责人转交路径是对同一 Bug 使用 `bug resolve`，携带当前已回读的 `resolution=fixed` 与 `resolvedBuild=16`，不改变最终 resolved 状态。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：同一个 Bug #11 用 `bug resolve --resolution fixed --resolved-build 16 --assignee gh26_xiaoli_20260825` 后回读仍为 `resolved/fixed`、负责人为小李；随后同一 Bug close 并回读为 `closed/fixed`，没有错误指回小王。诊断 Bug #12 仅用于兼容性探针，纳入最终清理清单。

### BUG-GH-008

- 测试结果：通过。
- 发现的问题：无新的源码问题；关闭前必须把状态、解决版本和当前负责人作为同一次用户意图的显式回读条件。
- 根因：无；公共 CLI 不做隐式写后查询，本场景由调用流程显式执行 `bug view`。
- 修改内容：无运行时代码修改；在 close 前再次读取同一个 Bug #13，确认 `resolved/fixed/resolvedBuild=16/assignedTo=小王`，未因不一致而操作其他对象。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：确认条件满足后仅关闭 Bug #13；关闭写入后立即回读为 `closed/fixed`，解决版本仍为 16，且没有创建或关闭替代 Bug。

### BUG-GH-009

- 测试结果：通过。
- 发现的问题：无新的源码问题；`not-repro` 解决不要求目标实例提供 `resolvedBuild`，后续 activate 仍按 21.7.8 兼容要求显式带回读的影响版本 16。
- 根因：无；这是目标版本对 activate 的既有参数要求。
- 修改内容：创建并回读 Build `4.2.1`（ID 17）；同一个 Bug #14 依次执行 `not-repro`、带 Windows 11/Chrome/连续登录三次备注的 activate、`fixed` 解决和 close。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#14 初始 `active/张三`，not-repro 后为 `resolved/notrepro/小王`；重新激活后为 `active/张三` 且备注保留复现环境与步骤；以 `4.2.1` fixed 解决并交小王，最终回读为 `closed/fixed`，全程仅使用同一 Bug。

### BUG-GH-010

- 测试结果：通过。
- 发现的问题：无新的源码问题；产品经理作为真实测试用户账号可接收 by-design 解决结果，重新激活仍需显式带当前影响版本 17。
- 根因：无；`bug.activate` 的显式 `openedBuild` 要求沿用已记录的 21.7.8 兼容性。
- 修改内容：创建并回读 Build `4.3.0`（ID 18）；同一个 Bug #15 依次执行 by-design 交产品经理、带产品验收结论的 activate 交张三、fixed 交小王和 close。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#15 回读历史包含 `active/张三 → resolved/bydesign/产品经理 → active/张三（产品结论备注）→ resolved/fixed/4.3.0/小王 → closed`，没有替代 Bug。

### BUG-GH-011

- 测试结果：通过。
- 发现的问题：无新的源码问题；external 解决和再次 fixed 解决均可不提供 resolvedBuild，只有 activate 按目标版本要求显式带当前影响版本 18。
- 根因：无；沿用 21.7.8 `bug.activate` 的真实兼容性约束。
- 修改内容：创建并回读 Build `5.0.2`（ID 19）；同一个 Bug #16 由李明处理，先 external 交小王，再用抓包结论激活交李明，最后 fixed 交小王。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#16 回读为 `resolved/external/小王 → active/李明` 且备注保留“第三方响应正常、代码超时处理有问题”，再回读为 `resolved/fixed/resolvedBuild=19/小王`；未创建第二个 Bug，按场景未额外执行 close。

### BUG-GH-012

- 测试结果：通过。
- 发现的问题：无新的源码问题；P0 由真实 `severity=1/priority=1` 表示，postponed 解决不要求版本，重新激活显式使用影响版本 19。
- 根因：无；沿用目标实例的 activate 版本兼容规则。
- 修改内容：创建并回读 Build `5.1.1`（ID 20）；同一个 Bug #17 依次 postponed、带项目经理 P0 结论的 activate 交李明、fixed 交小王。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#17 初始 `active/severity=1/李明`，postponed 后重新激活仍是同一 ID、负责人李明且备注保留；fixed 后回读为 `resolved/fixed/resolvedBuild=20/小王`，未创建新 Bug，按场景未执行 close。

### BUG-GH-013

- 测试结果：通过。
- 发现的问题：无新的源码问题；`will-not-fix` 在未指定 assignee 时目标实例会清空负责人，随后 activate 可按产品结论重新指定张三。
- 根因：目标 ZenTao lifecycle 语义；不是重试或新建对象。
- 修改内容：无运行时代码修改；同一个 Bug #18 依次执行 will-not-fix、带付费客户影响原因的 activate 交张三、fixed 交小王。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#18 回读历史为 `resolved/willnotfix → active/张三（产品负责人原因备注）→ resolved/fixed/resolvedBuild=20/小王`，始终为同一个 Bug，按场景未执行 close。

### BUG-GH-014

- 测试结果：通过。
- 发现的问题：无新的源码问题；duplicate 解决需要显式 `--duplicate-bug`，目标实例会清空 A 的负责人；Bug B 未被联动改变。
- 根因：目标 ZenTao duplicate lifecycle 的既有语义。
- 修改内容：创建并回读 Bug B #19（Firefox）和 Bug A #20（Chrome）；A 使用 duplicate 关联 B，再使用显式影响版本 20 激活 A，记录调用链/浏览器差异。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：A #20 回读为 `resolved/duplicate/duplicateBug=19 → active/张三`，备注保留“Chrome 可复现且调用链不同”；B #19 前后始终 `active/张三`，未改变 B 的生命周期。两个 Bug 均保留到最终清理。

### BUG-GH-015

- 测试结果：兼容性限制，未执行写入。
- 发现的问题：场景指定的既有 Bug #118“上传大文件时超时”和 #120 在真实 21.7.8 实例均不存在。
- 根因：目标实例没有 Issue 场景要求的固定业务数据，无法证明两者“均存在”或安全地把 #120 关联到 #118；不能用本轮新建的其他 ID 冒充指定对象。
- 修改内容：无；仅执行了 `bug view 118` 与 `bug view 120` 两次只读查询，均得到业务失败 `Bug does not exist.`，未发出 resolve/close/activate/delete。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；自动化结果不能替代缺失的真实前置数据。
- 场景复测结果：未达到 Gherkin 的 `Given` 前置条件，未伪造 duplicate 成功；记录为真实实例前置数据兼容性限制。

### BUG-GH-016

- 测试结果：通过。
- 发现的问题：无新的源码问题；目标实例允许同一 Bug 从 notrepro 再次激活并改为 fixed。
- 根因：无；activate 使用已回读的影响版本 20 满足 21.7.8 要求。
- 修改内容：创建并回读 Build `5.2.0`（ID 21）；同一个 Bug #21 先以 notrepro 交小王，激活交张三并记录连接池释放错误，再 fixed 交小王。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#21 回读历史完整包含 `resolved/notrepro → active/张三 → resolved/fixed/resolvedBuild=21/小王 → closed`；没有新建替代 Bug。

### BUG-GH-017

- 测试结果：通过（安全分支，无生命周期写入）。
- 发现的问题：无源码问题；真实 Bug #22 的解决版本回读为 Build #22 `5.3.0`，而用户所说的测试版本 `5.2.9` 不在当前执行的 Build 列表中且与解决版本不一致。
- 根因：无；这是场景要求的版本一致性判断。
- 修改内容：无；先执行 `bug view 22`，再执行 `build list --execution 9` 进行版本核对，明确提示应在 5.3.0 或包含修复的构建上回归，未调用 activate。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#22 保持 `resolved/fixed/resolvedBuild=22/assignedTo=小王`，没有因为错误版本测试而被错误驳回或改变其他 Bug。

### BUG-GH-018

- 测试结果：通过。
- 发现的问题：无新的源码问题；确认测试版本与 resolvedBuild 一致后，activate 是正确的生命周期动作。
- 根因：无；使用已回读的 Build 22（5.3.0）满足目标版本要求。
- 修改内容：无运行时代码修改；同一 Bug #22 以影响版本 22 激活并交张三，备注明确记录“5.3.0 仍可 100% 复现”。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#22 回读为 `active/assignedTo=张三/openedBuild=22`，历史保留 5.3.0 复现证据，没有创建相同问题的新 Bug。

### BUG-GH-019

- 测试结果：通过。
- 发现的问题：无新的源码问题；跨版本激活必须把原影响版本和新影响版本一起作为 repeatable `--affected-build` 传入，才能保留历史。
- 根因：无；目标 API 支持数组形式的 openedBuild，不能只传新版本覆盖旧版本。
- 修改内容：创建并回读 Build `5.1.0`（ID 23）和 `5.4.0`（ID 24）；Bug #23 先以 5.1.0 fixed 并 closed，重新出现后激活时传入 `[23,24]`、交张三。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#23 回读为 `active/张三/openedBuild=23,24`；历史明确记录旧版本 23 与新版本 24，原 Bug 未被拆分或新建重复对象。

### BUG-GH-020

- 测试结果：兼容性限制（构建内容关系不可观测），未执行生命周期写入。
- 发现的问题：真实 `build list` 能确认 Build `5.4.1`（ID 25）存在，但 ZenTao v2 Build/Release 公开字段没有“包含某主干提交”的可验证关系，也没有代码提交查询 endpoint；无法仅凭 API 证明主干修复是否进入 5.4.1。
- 根因：目标版本/当前 Skill endpoint catalog 不提供 commit-to-build 包含关系。
- 修改内容：无；创建并回读仅用于场景数据的 Build #25 和 active Bug #24，然后再次只读回读 Bug/Build，明确要求使用包含修复的构建，未执行 close 或 activate。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；Fake 能力不被当作真实构建包含证据。
- 场景复测结果：#24 保持 `active/assignedTo=小王/openedBuild=25`，没有伪造回归失败或修复结论；该场景的代码修复与正式构建区分需在真实 CI/代码仓库集成后继续验证。

### BUG-GH-021

- 测试结果：通过。
- 发现的问题：无新的源码问题；`trunk` 是当前 CLI 合同允许的合法特殊解决版本，不需要数字 Build ID。
- 根因：无；`bug.resolve` 的 build_reference 语义正确传递 `trunk`。
- 修改内容：无运行时代码修改；Bug #25 创建时影响版本为 trunk，resolve 时显式传 `--resolved-build trunk`，随后 close。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#25 回读为 `resolved/fixed/resolvedBuild=trunk/小王`，关闭后为 `closed/fixed`，未因非数字版本被拒绝。

### BUG-GH-022

- 测试结果：通过；目标版本没有在 Bug 创建合同中提供正式 `relatedBug` 参数，关联上下文以可回读文本记录。
- 发现的问题：无源码问题；两个独立现象必须使用不同 Bug ID，且原 Bug 关闭流程不能被新 Bug 覆盖。
- 根因：无；当前 v2 create 合同支持产品/项目/执行/影响版本，但不支持创建时写 `relatedBug`。
- 修改内容：原 Bug #26 以 fixed/5.4.0 解决后回读，创建新 Bug #27，使用同产品 3、项目 8、执行 9、Build 24，并在 keywords/steps 中保留“原 Bug #26 已解决、旋转为独立根因”上下文。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#26 回读为 `closed/fixed`，#27 回读为独立 `active` 且 ID 不同、产品/项目/执行/Build 均正确；没有把新现象改写为原 Bug，也没有新建多个重复对象。

### BUG-GH-023

- 测试结果：通过。
- 发现的问题：无新的源码问题；同一根因的另一入口应沿用原 Bug，不机械创建新对象。
- 根因：场景提供了日志证据指向同一异常链；由调用方显式决定复用原 Bug。
- 修改内容：创建并回读原 Bug #28，先 fixed/5.4.0 交小王，再以当前影响版本 24 激活同一 Bug，记录验证码入口仍复现及同一异常链。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#28 回读为 `resolved/fixed/小王 → active/张三/openedBuild=24`，激活历史保留验证码入口复现证据；没有创建新的回归 Bug。

### BUG-GH-024

- 测试结果：通过。
- 发现的问题：无新的源码问题；目标 API 支持重复 `--affected-build`，可在激活时保留原版本并加入新版本。
- 根因：无；使用 `[24,26]` 而不是仅传 26，避免覆盖原影响版本。
- 修改内容：创建并回读 Build `5.4.2`（ID 26）；Bug #29 先以 5.4.0 fixed/小王解决，再激活时传入 5.4.0 与 5.4.2、交张三并记录回归证据。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#29 回读为 `active/张三/openedBuild=24,26`，历史包含“5.4.2 回归仍可复现”，原影响版本未删除、未创建新 Bug。

### BUG-GH-025

- 测试结果：兼容性限制；真实 API 不支持“保持 active 仅转负责人”。
- 发现的问题：对同一个 active Bug #29 按场景发送 `PUT /bugs/29/activate`，请求包含 `assignedTo=gh26_liming_20260825`、当前影响版本 `24,26` 和转派备注；ZenTao 21.7.8 返回业务错误“状态不是已解决或已关闭的Bug不能激活”。
- 根因：`bug.activate` 是生命周期重新激活接口，只接受已解决或已关闭 Bug；`bug.edit` 的官方合同没有 `assignedTo`，目标实例对该 PUT 返回空响应且回读不变；真实实例没有可用的 active-Bug 独立转派 endpoint。为满足场景而先 resolve 再 activate 会违反场景“不应执行 resolve 或 close”，不能作为修复。
- 修改内容：没有增加伪造转派或自动生命周期动作；在 `bug.activate` 的 endpoint catalog、21.7.8 compatibility evidence、Bug reference 和验收文档中记录 active-only 转派限制。未重放失败写请求。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：写入失败后立即回读确认 #29 仍为 `active/张三`，`openedBuild=24,26`，没有新增 Bug、没有 resolve/close，也没有产生新的状态历史；按真实兼容性结果结束本场景。

### BUG-GH-026

- 测试结果：通过。
- 发现的问题：无新的源码问题；真实 API 支持在已解决 Bug 上激活并转交，再由新负责人解决并交测试，最后关闭同一个 Bug。
- 根因：无；沿用已回读的影响版本 26，满足 21.7.8 对 `bug.activate` 的显式 `openedBuild` 要求。
- 修改内容：创建并回读 Build `6.0.2`（ID 27）和 Bug #30；先由张三以 `fixed/26` 解决并交小王，回归失败后激活同一个 Bug 交李明并保存失败原因，再以 `fixed/27` 交小王并关闭。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#30 历史为 `resolved/fixed/26/小王 → active/李明（回归失败备注）→ resolved/fixed/27/小王 → closed`；三次写入后均回读确认，没有创建新 Bug。

### BUG-GH-027

- 测试结果：兼容性限制；目标实例无法执行 active Bug 的前后端责任转移。
- 发现的问题：独立真实 Bug #31 当前为 `active/张三`；按场景请求转给李明时，`PUT /bugs/31/activate` 返回“状态不是已解决或已关闭的Bug不能激活”。
- 根因：目标 ZenTao 21.7.8 只有生命周期 `activate` 接口可携带 `assignedTo`，但它要求当前状态为 resolved/closed；`bug.edit` 没有官方 `assignedTo` 且真实 PUT 无效，因此不存在保持 active 的受支持转派路径。
- 修改内容：无运行时代码修改；复用 BUG-GH-025 已记录的 `bug.activate` 兼容性事实，并保持错误原样返回；没有通过 resolve/close 改变场景要求的生命周期，也没有伪造后续“fixed/closed”。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：失败写入后回读 #31 仍为 `active/张三`，只保留 opened/assigned 历史，没有新 Bug 和生命周期副作用；后续步骤因首个真实能力缺失不虚报成功。

### BUG-GH-028

- 测试结果：兼容性限制；目标实例无法执行 active Bug 的后端到前端责任转移。
- 发现的问题：独立真实 Bug #32 当前为 `active/李明`；按场景转给张三时，`PUT /bugs/32/activate` 返回“状态不是已解决或已关闭的Bug不能激活”。
- 根因：与 BUG-GH-025、027 相同，`bug.activate` 是受状态约束的生命周期动作，`bug.edit` 不提供可用的 `assignedTo`；中途转派无法在不产生解决记录的情况下完成。
- 修改内容：无运行时代码修改；记录为 21.7.8 模块/API 兼容性限制，不执行场景禁止的 resolve/close，也不把失败写入伪装成一次转派或解决。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：失败后回读 #32 仍为 `active/李明`，没有新增转派/解决动作；后续固定版本 6.1.1 的步骤未虚报执行结果。

### BUG-GH-029

- 测试结果：兼容性限制；目标实例无法在 active 状态下把“后端已完成、前端继续处理”记录为仅转派。
- 发现的问题：独立真实 Bug #33 当前为 `active/李明`；请求只转给张三并保留 active 时，`PUT /bugs/33/activate` 返回“状态不是已解决或已关闭的Bug不能激活”。
- 根因：真实 ZenTao 21.7.8 没有可用的 active-Bug 负责人转派 endpoint，唯一带 `assignedTo` 的 `activate` 会改变生命周期且要求 resolved/closed；直接 resolve 会违反场景“不得提前解决”。
- 修改内容：无运行时代码修改；保持目标 API 错误和状态不变，记录兼容性限制，不用无依据的第二次写请求或隐式解决动作。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#33 回读仍为 `active/李明`，历史仅含创建和初始负责人；没有提前 resolve，也没有新 Bug。因首个能力在目标实例不可用，后续 6.2.0 fixed 步骤不冒充成功。

### BUG-GH-030

- 测试结果：兼容性限制；多团队联合流程的 active 状态负责人连续转派在目标实例不可用。
- 发现的问题：独立真实 Bug #34“App 修改头像后 Web 端不刷新”初始为 `active/张三`；第一次转给后端李明的真实请求即被 `PUT /bugs/34/activate` 拒绝，返回“状态不是已解决或已关闭的Bug不能激活”。
- 根因：该目标版本没有保持 active 的独立转派接口；`activate` 只能处理 resolved/closed 生命周期，无法表达 App→后端→Web 的中间责任链。
- 修改内容：无运行时代码修改；保留目标错误，不执行 resolve/close 或伪造三次负责人变更，记录为 21.7.8 兼容性限制。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：回读 #34 仍为 `active/张三`，没有中途生命周期变化、新 Bug 或虚假 QA 结果；后续步骤因第一步能力缺失不宣称成功。

### BUG-GH-031

- 测试结果：兼容性限制，同时验证安全拒绝行为。
- 发现的问题：独立真实 Bug #35 为 `active/张三`。当前 CLI 已不接受 `bug edit --assignee`，该尝试在发送业务 HTTP 前返回 `USAGE_ERROR`、exit code 2；目标实例也没有可用的 active-only 转派 endpoint。
- 根因：ZenTao 21.7.8 的 Bug 编辑合同不包含负责人字段，唯一可携带负责人的是受生命周期约束的 `activate`/`resolve`；使用 activate 会违反本场景明确禁止的动作。
- 修改内容：无运行时代码修改；沿用 BUG-GH-007 的永久修复（移除不受支持的 `bug edit --assignee`），确保不会为“只换负责人”隐式执行 activate、resolve、close 或 delete。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：回读 #35 仍为 `active/张三`，严重程度 2、优先级 2、影响版本 27 和历史均未改变；没有发送错误业务写请求，也没有伪造负责人已变更。

### BUG-GH-032

- 测试结果：兼容性限制；目标实例的 Bug 编辑接口对复现步骤更新不可用。
- 发现的问题：独立真实 Bug #36 初始为 `active/张三`，带有三步原始复现步骤。发送 `PUT /bugs/36` 补充 Safari 17.3 无痕模式步骤时，目标返回空 2xx 响应；CLI 按合同报告 `API_ERROR`，回读步骤仍为原三步。
- 根因：ZenTao 21.7.8 目标实例的 `PUT /bugs/{bugID}` 对已实测字段返回空响应且不改变对象，不能作为可靠的 Bug 编辑能力；这不是应通过重试掩盖的网络错误。
- 修改内容：无新的运行时代码修改；沿用已有空响应错误语义、禁止自动重试/回放，并保留 BUG-GH-007 对不支持负责人字段的永久修复。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：写后立即回读 #36 仍为 `active/张三`，原步骤、严重程度、优先级、影响版本和历史均未丢失；没有执行生命周期动作或创建新 Bug，记录为真实兼容性限制。

### BUG-GH-033

- 测试结果：兼容性限制；目标实例的 Bug 编辑接口不能可靠更新严重程度。
- 发现的问题：独立真实 Bug #37 初始严重程度为 3、负责人为李明、状态为 active。发送 `PUT /bugs/37` 将严重程度改为 1 时，目标返回空 2xx 响应；CLI 报告 `API_ERROR`，回读仍为严重程度 3。
- 根因：目标 ZenTao 21.7.8 的 `PUT /bugs/{bugID}` 对部分编辑字段统一表现为空响应且不落库，不能用“看起来成功”的 HTTP 状态推断字段已更新。
- 修改内容：无新的运行时代码修改；保持空响应错误、写请求不重试、不回放，并将该字段的真实观察补充到兼容性证据。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：回读 #37 仍为 `active/李明/严重程度 3`，负责人、优先级 2、影响版本 27、历史均未变化；没有执行生命周期动作或创建新 Bug。

### BUG-GH-034

- 测试结果：兼容性限制；目标实例的 Bug 编辑接口不能可靠更新优先级。
- 发现的问题：独立真实 Bug #38 初始优先级为 1、负责人为张三、状态为 active。发送 `PUT /bugs/38` 将优先级改为 3 时，目标返回空 2xx 响应；CLI 报告 `API_ERROR`，回读仍为优先级 1。
- 根因：目标 ZenTao 21.7.8 的 Bug PUT 编辑在真实实例上没有可确认的落库结果；HTTP 成功但空响应不能证明字段更新。
- 修改内容：无新的运行时代码修改；沿用明确的空响应错误语义，不自动延期、关闭、改变解决方案，也不重放写请求；补充兼容性证据。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：回读 #38 仍为 `active/张三/优先级 1/无解决方案`，严重程度 2、影响版本 27 和历史未变；没有生命周期副作用或新 Bug。

### BUG-GH-035

- 测试结果：通过。
- 发现的问题：无新的源码问题；CLI 本身不包含自然语言会话解析，因此用显式回读的同一真实 Bug ID 验证口语动作对应的 ZenTao 状态流。
- 根因：无；每次操作都绑定同一对象 #39，并沿用回读的影响版本 27，满足目标版本 activate 约束。
- 修改内容：创建并回读 Bug #39；依次执行 `resolved/fixed/小王`、同一 Bug `active/张三`、再次 `resolved/fixed/小王`、`closed`，备注分别保存“刚才那个”“打回去”“再给小王”等上下文动作。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#39 完整历史仅包含一个 Bug 的 `opened → resolved → activated → resolved → closed`，最终回读为 `closed/fixed/27`；没有切换到其他对象或创建新 Bug。

### BUG-GH-036

- 测试结果：通过。
- 发现的问题：无新的源码问题；两个真实 Bug 可通过负责人和标题上下文保持独立状态。
- 根因：无；创建并回读 Build `7.0.1`（ID 28），Bug A 为 #40“登录失败/张三”，Bug B 为 #41“注册失败/李明”。
- 修改内容：只对 Bug A #40 执行 `fixed/28/小王` 解决；对 Bug B 没有发送生命周期或转派写请求，保持其原 active/李明状态。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：A 回读为 `resolved/fixed/28/小王`；B 回读仍为 `active/李明`，历史只有创建和初始指派，证明上一轮没有串写到 B。

### BUG-GH-037

- 测试结果：通过。
- 发现的问题：无新的源码问题；先通过项目 Bug 列表只读查询按标题唯一定位目标，没有按负责人名直接猜测对象。
- 根因：无；创建并回读 Build `7.0.2`（ID 29），三个由张三负责的 active Bug 为 #42“登录超时”、#43“支付重复扣款”、#44“订单列表空白”。
- 修改内容：只对唯一匹配的 #43 执行 `fixed/29/小王` 解决；未向 #42、#44 发送任何写请求。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#43 回读为 `resolved/fixed/29/小王`；#42 和 #44 仍分别为 `active/张三`，原负责人、状态和历史均未变化。

### BUG-GH-038

- 测试结果：通过（歧义安全分支）。
- 发现的问题：无新的源码问题；真实用户列表允许两个不同账号使用同一真实姓名“王伟”。
- 根因：无；只读查询发现候选为账号 `gh26_wangwei1_20260825`（ID 8）和 `gh26_wangwei2_20260825`（ID 9），不能根据姓名猜测目标。
- 修改内容：创建并回读两个同名测试用户和 Bug #45；没有执行任何负责人写入，等待用户用账号等区分信息明确目标。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：回读 #45 仍为 `active/张三`，`openedBuild=29`，历史只有创建和初始指派；没有选择任一王伟、没有新状态动作或新 Bug。

### BUG-GH-039

- 测试结果：通过；真实复现 `UNKNOWN_WRITE_RESULT` 安全语义。
- 发现的问题：独立真实 Bug #46 的一次 `resolve` 请求经临时转发器提交到 ZenTao 21.7.8 后丢弃响应，CLI 返回 `UNKNOWN_WRITE_RESULT`。这不是服务器拒绝，而是客户端无法确认结果。
- 根因：写请求可能已在服务器执行；按源码合同，POST/PUT 不重试，不能把未知结果当作失败再次发送。
- 修改内容：无运行时代码修改；用正常配置显式 `bug view 46` 回读，不重放 `resolve`。
- 自动化测试结果：`tests.e2e.test_cli_error_semantics` 9/9 通过，覆盖 `commit_then_drop` 无重试、无后续隐式请求。
- 场景复测结果：回读确认 #46 已为 `resolved/fixed/29/小王`，历史只有一次 `resolved` 动作；没有重复生命周期变更。若回读未生效，才会在用户意图仍明确时由调用者决定下一次写入，本次无需再次写。

### BUG-GH-040

- 测试结果：通过（只读安全分支）。
- 发现的问题：无新的源码问题；“看看/处理一下”没有被推断成任意生命周期写操作。
- 根因：无；明确目标真实 Bug #47 后只执行两次 `bug view`，没有调用 resolve、activate、close 或 delete。
- 修改内容：无运行时代码修改；沿用 CLI 一条命令一个明确 endpoint 和 R3 删除必须显式 `--yes` 的安全合同。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：两次回读均为 `active/张三`、无解决方案、历史动作数为 2（opened/assigned），字段和生命周期完全未变；没有把“清理/处理”解释为 delete。

### BUG-GH-041

- 测试结果：通过，包含真实关联字段和版本兼容性处理。
- 发现的问题：创建前置研发需求时，目标 21.7.8 对官方标为可选的 `reviewer` 返回“『评审人』不能为空”；补充真实评审人 admin 后创建成功。没有猜测关联 ID。
- 根因：目标实例对 `story.create` 的 `reviewer` 有额外必填校验；这是版本兼容差异，不是 Bug 创建逻辑的吞错问题。
- 修改内容：用显式补充 `--reviewer admin` 的请求创建并回读需求 #4；再创建并回读任务 #3（`execution=9, story=4, assignee=张三`），最后创建 Bug #48 时显式传入 `product=3, project=8, execution=9, story=4, task=3, assignee=张三`。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；真实前置请求和修正请求均有服务端响应/回读证据。
- 场景复测结果：#48 回读为 `product=3/project=8/execution=9/story=4/task=3/active/张三`；随后只回读同一个 Bug，没有创建第二个 Bug，也没有因关联对象存在多个候选而改用猜测 ID。

### BUG-GH-042

- 测试结果：通过。
- 发现的问题：无新的源码问题；真实 ZenTao 接受一次创建请求中的多个重复 `--affected-build` 值，并在详情中保留顺序集合。
- 根因：无；已通过只读回读确认正式版本 ID 23=`5.1.0`、ID 20=`5.1.1`，另以语义值 `trunk` 传入，没有为每个版本重复创建对象。
- 修改内容：只创建真实 Bug #49，发送 `openedBuild=[trunk,23,20]`，随后显式 view 回读。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#49 回读 `openedBuild=trunk,23,20`，状态 active、负责人张三，历史只有一次 opened/assigned；只存在一个 Bug，三个影响版本均保留。

### BUG-GH-043

- 测试结果：通过。
- 发现的问题：无新的源码问题；关系 ID 0 在 Bug 创建合同中表示合法的“无研发需求关联”。
- 根因：无；按场景显式传入 `--story 0`，没有从相似需求列表中猜测关联对象。
- 修改内容：只创建真实 Bug #50，随后通过 `bug view` 回读关联字段。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；现有语义参数回归已覆盖 zero relation ID。
- 场景复测结果：#50 回读 `story=0/task=0/product=3/project=8/execution=9`，状态 active、负责人张三；没有强行绑定需求，也没有创建第二个 Bug。

### BUG-GH-044

- 测试结果：兼容性限制；目标实例的 Bug 编辑接口不能执行需求解绑。
- 发现的问题：真实 Bug #51 初始 `story=4/task=3/active/张三`。发送 `PUT /bugs/51`、请求 `story=0` 时目标返回空 2xx 响应，CLI 报告 `API_ERROR`；回读仍为 `story=4`。
- 根因：目标 ZenTao 21.7.8 的 `bug.edit` 对关系字段也返回空响应且不落库；不能把空响应当成功，也不能通过删除需求或新建 Bug 绕过。
- 修改内容：无新的运行时代码修改；保持空响应错误语义、不重试、不删除需求、不创建替代 Bug，并将 #51 观察补入兼容性证据。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：回读 #51 仍为 `active/张三/story=4/task=3`，title、severity 2、priority 2、openedBuild 29 和历史未变；记录为真实兼容性限制。

### BUG-GH-045

- 测试结果：兼容性限制；目标实例的 Bug 编辑接口不能调整执行归属。
- 发现的问题：真实 Bug #52 初始属于项目 8 的迭代 A（execution 9），负责人张三、状态 active。发送 `PUT /bugs/52` 将 execution 改为迭代 B（ID 10）时返回空 2xx 响应；CLI 报告 `API_ERROR`，回读仍为 execution 9。
- 根因：目标 ZenTao 21.7.8 的 `bug.edit` 对 execution 关系字段不落库；空响应不是可确认成功，不能继续猜测覆盖其他字段。
- 修改内容：无新的运行时代码修改；不重试、不新建 Bug、不执行生命周期动作，并把该真实观察补入既有 `bug.edit` unsupported 证据。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#52 回读仍为 `active/张三/project=8/execution=9/story=0/severity=2/priority=2/openedBuild=29`，title 和历史均未变；记录为真实兼容性限制。

### BUG-GH-046

- 测试结果：兼容性限制；目标实例不能通过 `bug.edit` 增量修改复现步骤。
- 发现的问题：真实 Bug #53 初始为 `active/张三`，原步骤三行。发送补充英文模式、CSV 导入和第三步报错的 `PUT /bugs/53` 时返回空 2xx 响应；CLI 报告 `API_ERROR`，回读仍为原步骤。
- 根因：目标版本的 Bug 编辑 PUT 对 steps 字段没有可确认的落库行为；不能把空响应误判为成功，也不能用生命周期写操作代替字段编辑。
- 修改内容：无新的运行时代码修改；不重试、不执行 resolve/activate/close，并把 #53 作为真实 steps 不支持观察记录。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：回读 #53 仍为 `active/张三`，步骤、severity 2、priority 2、openedBuild 29 和历史均保持原值；没有创建新 Bug。

### BUG-GH-047

- 测试结果：兼容性限制；目标实例不能通过 `bug.edit` 同时更新标题、严重程度、优先级和类型。
- 发现的问题：真实 Bug #54 初始关联 `story=4/task=3/project=8/execution=9/openedBuild=23,20`、负责人张三、状态 active。请求四个目标字段后返回空 2xx 响应；CLI 报告 `API_ERROR`，回读四个字段均保持原值。
- 根因：目标 ZenTao 21.7.8 的 Bug PUT 编辑整体没有可确认落库结果，不能根据 HTTP 2xx 判断字段已写入。
- 修改内容：无新的运行时代码修改；不重试、不进行字段覆盖补偿、不改变生命周期，记录真实 `bug.edit` unsupported 证据。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#54 回读仍为 `active/原标题/severity=2/priority=3/type=performance`，需求、任务、项目、执行、影响版本和负责人均未漂移；没有创建替代 Bug。

### BUG-GH-048

- 测试结果：通过，含 ZenTao 富文本的表示层规范化。
- 发现的问题：无源码问题；真实服务端将 URL 自动转为同源 `<a href>`，并把查询参数中的 `&` 转为 HTML `&amp;`，但保留 URL、换行、中文、引号、方括号和代码片段语义。
- 根因：这是 ZenTao 富文本字段的服务端展示规范化，不是 CLI 参数解析或截断；创建请求仍将完整多行文本作为一个 `--steps` 值发送。
- 修改内容：无运行时代码修改；创建 Bug #55 后只读回读并检查行数、URL href 和代码片段，没有调用不支持的 bug.edit。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#55 回读为 `active/张三`，steps 长度 320，三行结构、中文说明、URL 链接、JSON 引号、`[A-1]` 和 `curl -H` 代码均可见；没有截断、误拆参数或创建第二个 Bug。

### BUG-GH-049

- 测试结果：通过。
- 发现的问题：无新的源码问题；用户未提供 severity、priority、负责人、项目或执行时，CLI 没有擅自补值，服务端按自身规则返回默认值。
- 根因：无；创建请求只包含明确的 `product=3/title/affectedBuild=trunk` 业务字段。
- 修改内容：只创建真实 Bug #56，随后回读最终对象；没有在客户端构造默认严重程度或优先级。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#56 回读为 `product=3/openedBuild=trunk/status=active/severity=3/priority=3`，负责人为空、项目和执行为 0；报告的是服务端实际默认值，没有描述为用户指定值。

### BUG-GH-050

- 测试结果：通过，明确记录语义边界。
- 发现的问题：真实 `bug.resolve --resolution to-story` 只返回 Bug 保存成功，没有返回新需求 ID，也没有自动创建需求。
- 根因：`to-story` 是 Bug 的解决方案状态，不等价于 `story.create`；当前 API 调用链没有需求创建动作。
- 修改内容：创建并回读 Bug #57，执行一次 `to-story` 解决；随后只读查询产品需求列表验证是否出现 GH26-050 新需求，没有编造编号或内容。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#57 回读为 `resolved/tostory/story=0`，产品需求列表中没有 GH26-050 对应新对象；真实报告为“Bug 已按转为需求解决，但未创建/未返回新需求”，没有声称需求已创建。

### BUG-GH-051

- 测试结果：通过（目标不存在安全分支）。
- 发现的问题：无新的源码问题；人员目录只读查询没有返回账号或真实姓名“赵六”。
- 根因：无；目标在当前 ZenTao 人员目录中不存在，不能将其猜测为张三、小王或其他相似账号。
- 修改内容：创建并回读 Bug #58 后，仅查询人员目录；没有调用 activate、resolve、close 或任何不受支持的负责人编辑接口。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#58 仍为 `active/张三`，历史只有 opened/assigned；明确报告“赵六不存在/无法解析”，没有声称指派成功，也没有用生命周期状态绕过人员解析。

### BUG-GH-052

- 测试结果：兼容性/权限限制；真实低权限账号无法满足“可查看但不可创建”的精确前置条件，但权限拒绝路径已真实验证。
- 发现的问题：新建账号 `gh26_nobug_20260825` 登录后读取 CRM 产品和创建 Bug 均收到 HTTP 403 `Not allowed`；创建命令返回 `API_ERROR`，没有返回成功对象。
- 根因：该测试账号在目标 ZenTao 21.7.8 没有 CRM 产品访问/创建权限，当前公开 CLI 没有为该账号授予产品级权限的接口；不能把“看不到产品”伪装成只缺创建权限。
- 修改内容：无运行时代码修改；保留真实 403 错误，不切回 admin 重试、不改投其他产品；仅用 admin 做只读确认。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：admin 只读查询 CRM Bug 列表中没有 GH26-052 对象；未声称创建成功，未产生第二次写入。该条按真实权限限制记录，精确的“可读/不可写”分层权限仍需目标实例提供相应授权配置后复验。

### BUG-GH-053

- 测试结果：兼容性/权限限制；真实账号无法满足“可读可编辑但不可 resolve”的精确前置条件，但 resolve 403 路径已验证。
- 发现的问题：独立真实 Bug #59 为 `active/张三`。新建的 `gh26_nolifecycle_20260825` 执行 resolve 时收到 HTTP 403 `Not allowed`，CLI 返回 `API_ERROR`；没有把拒绝降级成 edit，也没有执行 close。
- 根因：目标实例对该测试账号没有 Bug 访问/生命周期权限，当前公开 CLI 没有配置细粒度产品/动作权限的接口；不能声称账号具备场景要求的普通编辑能力。
- 修改内容：无运行时代码修改；保留真实权限错误，不切换 admin 重试 resolve，不伪造“已交小王”。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：admin 只读回读 #59 仍为 `active/张三`、无 resolution/resolvedBuild，历史只有 opened/assigned；没有直接 close 或其他替代写入。精确分层权限结果待目标实例授权配置后复验。

### BUG-GH-054

- 测试结果：通过（目标不存在安全分支）。
- 发现的问题：真实 `bug view 4321` 返回业务失败 `Bug does not exist.`，CLI 保留为 `API_ERROR`，exit code 1。
- 根因：目标对象在 ZenTao 21.7.8 中不存在或当前账号不可访问；不能根据附近编号推断对象。
- 修改内容：只执行一次明确的只读 view；没有创建替代 Bug、没有尝试 resolve、没有改用相邻对象。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：保留服务端 404 等价业务错误语义，未输出“已修复/已交小王”之类成功结论。

### BUG-GH-055

- 测试结果：通过（输入校验安全分支）。
- 发现的问题：执行 9 的真实构建只读列表中没有名称 `9.9.9`；CLI 的 `--resolved-build` 合同只接受正整数构建 ID 或 `trunk`，因此该值在发出业务 HTTP 前返回 `USAGE_ERROR`“必须是整数”。
- 根因：当前 API/CLI 使用构建 ID，不支持将任意不存在的版本名称直接作为 resolvedBuild 发送；不能猜测成 trunk、20 或其他相近构建，也不应绕过合同做未经确认的原始请求。
- 修改内容：无运行时代码修改；保留输入错误，不重试、不改用其他版本、不声称解决成功。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#60 回读仍为 `active/张三`，`resolution` 和 `resolvedBuild` 为空，历史未增加；本次没有向 ZenTao 发出错误版本写入，真实服务端状态保持不变。若要验证服务端对“合法格式但不存在 ID”的错误，需另有明确的版本 ID 语义与目标实例授权。

### BUG-GH-056

- 测试结果：兼容性限制；真实目标 API 接受跨产品关系，已清理错误 fixture。
- 发现的问题：CRM 产品 #3、CRM 项目 #8 与 ERP 产品 #4、ERP 迭代 #12、ERP 需求 #5 的组合被发送到 `bug.create` 后，ZenTao 21.7.8 返回成功并创建 #61；回读确认 `product=3/project=8/execution=12/story=5`，即关系错误但被服务端接受。
- 根因：源码按 CLI 明确提供的字段逐一映射到官方 `productID/project/execution/story`，没有猜测或偷偷替换；目标模块本身不做跨产品一致性校验。当前 CLI 合同要求一条命令对应一个明确业务 endpoint，也没有本地关系缓存，不能在不增加隐式 endpoint 行为的情况下把服务端缺失的校验伪装成已支持能力。
- 修改内容：没有增加硬编码 ID、隐式改投或自动补偿写入；在 Bug reference、21.7.8 compatibility evidence 和验收文档中记录调用方必须先显式只读验证关系。按安全要求用明确 `bug delete 61 --yes` 删除错误 probe，并 view 回读确认 `Bug does not exist.`。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；无运行时代码修改。
- 场景复测结果：删除后只读确认 #61 不存在，没有保留任何被错误宣称成功的 CRM/ERP 关联 Bug，也没有创建同名替代对象。真实结论为目标模块兼容性限制：关系校验必须在用户/AI 工作流中先完成。

### BUG-GH-057

- 测试结果：通过（旧页面并发指令去重）。
- 发现的问题：无新的源码问题；目标版本的 activate 只能从 resolved/closed 进入 active，因此第二个旧页面指令不能再次发送同一 lifecycle 写入。
- 根因：无；第一次回归已通过同一 Bug #62 完成 `resolved/fixed/29/小王 → active/张三`，第二个测试上下文先显式 view 得到当前 active 状态。
- 修改内容：第二条“也失败了”只做当前状态确认，没有调用 activate、没有创建第二个 Bug；保留第一位测试人员的真实备注，并准确说明第二条证据未写入 ZenTao 的历史。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#62 最终 `active/张三`，总动作 4、`activated` 次数 1；没有重复 `resolved→active`，没有产生新对象。第二个旧页面不会覆盖当前状态，需后续用户明确追加证据时再走受支持流程。

### BUG-GH-058

- 测试结果：通过（过期上下文冲突安全分支）。
- 发现的问题：无新的源码问题；真实 Bug #63 在张三旧回复前已由同一 Bug 转为 `active/李明`。
- 根因：无；先通过 `resolved/fixed/29/小王 → active/李明` 建立并回读当前协作状态，随后对张三声称“7.0.1 已修好”的旧上下文先 view，而不是直接 resolve。
- 修改内容：没有覆盖当前负责人、没有静默执行生命周期写入；将旧声明与当前 `active/李明` 的冲突明确呈现，等待用户确认后续意图。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#63 回读负责人仍为李明、状态 active、历史只有一次转交；没有因为旧上下文把负责人改回张三或声称已交小王。

### BUG-GH-059

- 测试结果：通过；真实复现创建写入结果未知且无重复创建。
- 发现的问题：Build `7.1.0`（ID 30）已回读后，一次真实 `bug.create` 请求经转发器提交到 ZenTao 后丢弃响应，CLI 返回 `UNKNOWN_WRITE_RESULT`。
- 根因：创建请求可能已执行，消息重发不能无条件再次 POST；必须先按唯一上下文只读确认。
- 修改内容：没有重发 create；使用正常配置按产品和唯一标题查询 Bug 列表，确认已有对象后继续使用其 ID。
- 自动化测试结果：`tests.e2e.test_cli_error_semantics` 9/9 通过，覆盖 commit-then-drop 写入不重试；catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：只存在一个匹配对象 #64，回读为 `product=3/project=8/execution=9/openedBuild=7.1.0/active/张三`；没有产生第二个重复 Bug，也没有重放未知写请求。

### BUG-GH-060

- 测试结果：通过；真实复现 resolve 响应丢失。
- 发现的问题：Build `7.2.1`（ID 31）和 Bug #65 已准备；真实 resolve 请求提交后连接中断，CLI 返回 `UNKNOWN_WRITE_RESULT`。
- 根因：写请求结果未知时不能把客户端异常当作服务端失败，也不能自动重复生命周期写入。
- 修改内容：没有重放 resolve；正常配置下只读 view #65，依据真实状态继续处理，不改变当前生命周期。
- 自动化测试结果：`tests.e2e.test_cli_error_semantics` 9/9 通过；catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#65 回读为 `resolved/fixed/31/小王`，历史中 `resolved` 次数为 1；确认第一次已生效，未重复 resolve、未制造重复副作用。

### BUG-GH-061

- 测试结果：兼容性/安全合同限制；真实 401 路径通过，但当前系统不自动刷新 Token 重放 PUT。
- 发现的问题：真实 Bug #66 和 Build `7.3.0`（ID 32）已准备；第一次 resolve 请求经转发器使用过期 Token 发送到 ZenTao，目标返回 HTTP 401 `Not allowed`，CLI 原样返回 `API_ERROR`。
- 根因：仓库当前写入安全合同明确禁止 POST/PUT/DELETE 自动重试；401 发生时无法证明原写请求是否到达并执行，自动刷新认证后重放会制造重复生命周期风险。该场景要求的“一次刷新后重放”与现行安全合同冲突。
- 修改内容：无运行时代码修改；没有刷新后重放、没有改用 edit/close，也没有把权限错误描述为 resolve 成功。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；现有写入 HTTP 错误无重试测试通过。
- 场景复测结果：只读回读 #66 仍为 `active/张三`，无 resolution/resolvedBuild，历史未增加；真实结论是 Token 失效后的自动写入恢复能力当前不支持，需后续明确改变安全合同并补充幂等凭据后再设计。

### BUG-GH-062

- 测试结果：通过安全分支；真实 Bug 状态未被服务端错误绕过。5xx 本身由受控转发器注入，未冒充 ZenTao 原生故障。
- 发现的问题：真实 Bug #67 和 Build `7.4.0`（ID 33）已准备；resolve 路径收到一次注入 HTTP 500，CLI 返回 `API_ERROR`，没有发送 edit/close 或第二次 resolve。
- 根因：写入 HTTP 5xx 不能安全推断服务器是否执行；现行合同要求保留真实失败/未知语义，禁止无依据重试和生命周期绕过。
- 修改内容：无运行时代码修改；只读回读 #67，保留真实目标状态，未把代理注入响应描述为 ZenTao 自身 5xx 能力。
- 自动化测试结果：`tests.contract.test_error_semantics` 13/13 通过；覆盖写入 HTTP 错误不重试；catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：#67 回读仍为 `active/张三`，无解决方案/解决版本，历史未增加；符合“报告真实失败、不用其他动作绕过”。目标实例原生 5xx 的复验仍需可控服务端故障注入环境。

### BUG-GH-063

- 测试结果：通过。
- 发现的问题：无新的源码问题；关闭只影响未关闭列表，不会删除历史对象。
- 根因：无；同一真实 Bug #68 先 `resolved/fixed/33/小王`，再 close。
- 修改内容：执行一次 close 后显式回读详情、产品 `unclosed` 列表和 `all` 列表，没有调用 delete。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：详情为 `closed/fixed/33`；未关闭列表不含 #68，全部列表仍含 `#68/closed`，证明列表过滤消失不是删除。

### BUG-GH-064

- 测试结果：兼容性限制；已通过显式宽分页和回读后状态过滤得到正确结果。
- 发现的问题：真实执行 #9（`Iteration A 20260825`）请求 `browseType=unresolved`、每页 20 条时，目标仍返回 20 条，其中包含 8 个 `closed` 和多个 `resolved` Bug；服务端未按 browse 语义过滤。
- 根因：目标 ZenTao 21.7.8 的 execution Bug 列表过滤参数与实际响应不一致。源码正确发送 `browseType=unresolved`，但不能把服务端返回的 closed/resolved 行当成未关闭结果。
- 修改内容：无运行时代码修改；将 `bug.list_execution` 真实观察登记为兼容性差异。用 `recPerPage=100` 重新读取同一 execution，再按回读的 `status` 排除 `resolved/closed`，按 `severity/priority/id` 排序并取前 20；没有跨 execution 或按标题混入对象。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；无运行时代码修改。
- 场景复测结果：宽分页回读 61 条、`pageTotal=1`，过滤后前 20 条全部属于 execution 9、状态 active，严重程度/优先级排序可见；报告了首轮每页 20 的分页与服务端过滤失真，没有把 closed Bug 混入结果。

### BUG-GH-065

- 测试结果：通过。
- 发现的问题：无新的源码问题；目标 21.7.8 支持从 closed Bug 激活，并接受重复 affected-build 以保留旧版本和新增版本。
- 根因：无；创建并回读 Build `8.0.0`（ID 34），同一 Bug #69 先 `resolved/fixed/33/小王`、close，再用 `[33,34]` 激活交张三。
- 修改内容：没有创建同标题替代 Bug；激活后显式 view，并通过产品未关闭列表查同一 ID。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；沿用已验证的 activate 版本兼容性。
- 场景复测结果：#69 从 `closed` 变为 `active/张三`，详情 `openedBuild=33,34`，历史只有一次 activated；未关闭列表再次找到 #69（展示版本名 `7.4.0,8.0.0`），没有新对象。

### BUG-GH-066

- 测试结果：通过。
- 发现的问题：无新的源码问题；真实产品 #3 与项目 #8 均显示名称 `CRM`，两个 scope 的结果集合不同。
- 根因：无；按用户语义分别调用 `bug list --project 8` 和 `bug list --product 3`，没有用名称猜测 scope。
- 修改内容：两次都只读查询并回读集合：项目 scope 返回 62 条，产品 scope 返回 63 条；产品独有 #56（无项目关联）证明范围确实不同。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过。
- 场景复测结果：项目请求只使用 `project=8`，产品请求只使用 `product=3`；两次结果均保留真实字段，没有把产品结果误当项目结果或反向串 scope。

### BUG-GH-067

- 测试结果：通过。
- 发现的问题：无新的源码问题；删除安全门槛和对象不存在语义均符合合同。
- 根因：无；真实 fixture #70 创建后先 view 确认了精确 ID、产品、项目和执行范围。
- 修改内容：第一次 `bug delete 70` 缺少 `--yes` 时返回 `USAGE_ERROR`、exit 2；随后仅对明确的 #70 执行一次 `bug delete 70 --yes`，没有扩大到同标题或同产品对象。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；已有 delete `--yes`/无 HTTP 回归覆盖通过。
- 场景复测结果：删除成功后 `bug view 70` 返回服务端 `Bug does not exist.` 业务错误，准确确认 #70 已不可读取；没有用 close 替代 delete，也没有删除其他 Bug。

### BUG-GH-068

- 测试结果：通过安全分支；没有执行 destructive 写入。
- 发现的问题：真实产品 #3 当日共有 63 条 Bug，当前均带本轮 `GH26-` 标识；但用户只说“把今天测试的 Bug 清理掉”，没有提供逐对象删除意图。只读查询本身不能把模糊请求升级成批量删除授权。
- 根因：删除属于 R3 destructive 操作，标题前缀、创建日期和当前测试上下文都不能替代明确对象范围；即使本次查询没有发现非 `GH26-` 条目，也不能据此猜测用户要删除全部候选。
- 修改内容：仅执行 `bug list --product 3 --browse all --per-page 100`，汇总日期、标题标识和对象 ID；没有调用 delete，没有按标题/日期批量处理，也没有删除人工业务对象。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；无运行时代码修改。
- 场景复测结果：只读结果为 `total=63/today=63/GH26=63/non-GH26=0`，destructive 写入数为 0；已向用户保留“需要明确逐对象 ID 后再删除”的安全边界。

### BUG-GH-069

- 测试结果：通过；按实际 ZenTao ID 执行两个明确的单对象删除。
- 发现的问题：Issue 示例中的 #708、#709、#710 在当前真实实例均不存在，不能伪造这些 ID 的状态。
- 根因：示例 ID 是场景数据，不是当前实例可预先假设的固定对象；真实操作必须先 view/创建并使用服务端实际返回的 ID。
- 修改内容：创建并回读等价对象 #71、#72（测试）及 #73（同产品保留控制对象），随后只发送 `delete 71 --yes` 和 `delete 72 --yes` 两个单对象请求；没有把 #73 放入删除范围。没有失败重试，也没有扩大删除范围。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；沿用 delete `--yes` 和无隐式重试回归覆盖。
- 场景复测结果：#71、#72 删除后 view 均返回 `Bug does not exist.`；#73 仍为 `active`、标题 `GH26-069 人工业务 Bug 保留控制 20260825`、产品 #3、项目 #8、执行 #9，确认未被修改。

### BUG-GH-070

- 测试结果：通过；清理范围严格限定为本轮唯一 run 标识的 fixture。
- 发现的问题：无新的源码问题；当前实例允许对明确 Bug ID 进行受保护删除，并能通过 view 验证对象消失。
- 根因：无；清理对象由本轮创建响应返回的 #74、#75 及唯一标题 `GH26-RUN20260825-070` 共同证明，不能用产品、日期或模糊标题推导范围。
- 修改内容：创建并回读 #74、#75 后，在授权清理阶段只执行 `delete 74 --yes`、`delete 75 --yes`；每个对象分别回读，未按产品或日期批量删除，也没有用更多删除补偿失败。
- 自动化测试结果：catalog/official/contract/scenario/CLI error 25/25 通过；无运行时代码修改。
- 场景复测结果：#74、#75 删除后 view 均返回 `Bug does not exist.`；此前已存在的控制对象 #73 仍保持 `active` 且字段未变化。清理结果按实际回读确认，没有把其他历史测试对象或控制对象宣称为已清理。

## 场景完成后的显式 teardown

- 在 070 场景完成后，依据本次逐条记录中保存的创建 ID，另行执行了逐对象 fixture teardown：#6–#60、#62–#69；每个 ID 都先属于本轮 `GH26-*` 测试对象，delete 请求成功后立即 view，全部返回 `Bug does not exist.`。
- 该 teardown 没有使用产品、日期、标题模糊匹配或批量删除 endpoint；任一对象失败会立即停止，实际没有发生失败或重试。
- 已先前删除并回读确认不存在的 probe/fixture 为 #4、#5、#61、#70、#71、#72、#74、#75；明确保留的 #73 是 069/070 的人工业务控制对象，当前仍为 `active`，未进入 teardown 删除范围。

## 最终汇总

- 70 个场景均按 `BUG-GH-001` → `BUG-GH-070` 顺序完成，没有跳过或伪造成功：47 个通过，23 个按真实 ZenTao 21.7.8 的兼容性/权限/安全合同限制记录；没有未处理的场景失败。
- 兼容性/权限/安全限制场景：`002, 007, 015, 020, 025, 027, 028, 029, 030, 031, 032, 033, 034, 041, 044, 045, 046, 047, 052, 053, 056, 061, 064`。其中 002、007 同时完成了源码/合同修复或安全语义收紧并复测；列表中的“兼容性”表示目标实例能力边界仍被如实保留，不表示测试被跳过。
- 实际运行时修复：`bug.create` 同时发送 `productID` 与 21.7.8 所需的兼容字段 `product`；移除目标实例对 `bug.edit` 不支持且回读不变的负责人参数，改为明确拒绝并引导使用受支持的生命周期转交；同步 Fake、endpoint catalog、官方合同快照、兼容性证据、CLI/contract/scenario 回归测试和文档。
- 真实验证命令：定向回归 25/25 通过；`python3 skills/zentao/tests/run_all.py` 运行 88 tests、`OK`，Catalog/Internal/CLI/Skill routes/Fake API/Contract tests/CLI E2E/Official snapshot 均 `120/120`，`21.7.8 observed=26/120`、`unsupported=17/120`、`not observed=77/120`，`Real API calls: 0`，`Result: PASS`。
